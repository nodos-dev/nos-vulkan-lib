list(APPEND DEPENDENCIES vkl spirv-cross-core)
list(APPEND INCLUDEFOLDERS ${DYNALO_INCLUDE_DIR} ${CMAKE_CURRENT_SOURCE_DIR})

add_folder_as_project(mzVulkan STATIC "${DEPENDENCIES}" "${INCLUDEFOLDERS}")
